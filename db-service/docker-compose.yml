version: '3.8'
services:
  data_db:
    image: postgres
    env_file:
      - data_db.env
    platform: linux/amd64
    container_name: data_db
    volumes:
      - db_volume:/var/lib/postgresql/data/  # Path is where the container stores sql data
    ports:
      - 5432:5432
    networks:
      - cmsc495_network

  auth_db:
    image: postgres
    platform: linux/amd64
    container_name: auth_db
    volumes:
      - db_volume:/var/lib/postgresql/data/  # Path is where the container stores sql data
    ports:
      - 5432:5432
    networks:
      - cmsc495_network

volumes:
  db_volume:
    external: true
    name: db_volume

networks:
  cmsc495_network:
    external: true
    name: cmsc495_network
